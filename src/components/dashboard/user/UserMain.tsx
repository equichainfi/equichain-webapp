import { SortViewOptions } from "@/components";
import Header from "./Header";

export default function UserMain() {
    return (
        <main className="w-screen">
            <Header />
            <SortViewOptions />
        </main>
    );
}
